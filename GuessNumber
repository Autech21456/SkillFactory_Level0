# Задается число от 1 до 100,
# необходимо его угадать за минимальное количество циклов работы программы

# 1) Задание числа случайным образом
# 2) Определение загаданного числа способом поразрядной компенсации степенями числа 2,
# таким образом количество циклов не должно превысить 7.

import numpy as np

i_search = 0
number = np.random.randint(1,101)   # загадали число
print ("Загадано число от 1 до 100,", str(number))

for i in range(6,-1,-1):
    if number - i_search >= 2**i:
        i_search += 2**i
    if number - i_search == 0:
        break

print('Результат:', str(i_search))